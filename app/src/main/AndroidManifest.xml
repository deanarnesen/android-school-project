<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cs340.tickettoride">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_ticket_to_ride"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".EndGameActivity"
            android:label="@string/title_activity_end_game"></activity>
        <activity
            android:name=".MapGameActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CreateGameActivity"
            android:label="@string/title_activity_create_game"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GameLobbyActivity"
            android:label="@string/title_activity_game_lobby"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ListGameActivity"
            android:label="@string/title_activity_lobby"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>